using System;
using System.Collections;
using System.Collections.Generic;

public class Testing
{
    // static void Main(string[] args)
    // {
    //     MiniBoard board = new MiniBoard();
    //     Console.WriteLine("miniBoard is finished (false): " + board.isFinished());
    //     board.setSpace(0, 0, Tools.piece.x);
    //     board.setSpace(0, 1, Tools.piece.x);
    //     board.setSpace(0, 2, Tools.piece.x);
    //     Console.WriteLine("miniBoard is finished (true): " + board.isFinished());
    //     UltimateBoard bigBoard = new UltimateBoard();
    //     Console.WriteLine("bigBoard is finished (false): " + bigBoard.isFinished());
    //     MiniBoard[][] bigBoardArr = new MiniBoard[3][];
    //     for (int r = 0; r < 3; r++)
    //     {
    //         bigBoardArr[r] = new MiniBoard[3];
    //         for (int c = 0; c < 3; c++)
    //         {
    //             bigBoardArr[r][c] = new MiniBoard(board.getBoard());
    //         }
    //     }
    //     bigBoard = new UltimateBoard(bigBoardArr);
    //     Console.WriteLine("bigBoard is finished (true): " + bigBoard.isFinished());
    // }

}